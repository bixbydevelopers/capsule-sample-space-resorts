// TODO: Should be property result, but that doesn't work
dialog (Result) {
  match: Gravity (this)
  if (size(this) == 1) {
    template ("The gravity is #{value(this)}.")
    // template ("중력은 #{value(gravity)}입니다.")
  } else {
    // TODO
    template (TODO)
  }
}