dialog (Result) {
  match: SpaceResort (this)
  switch (plural(this)) {
    case (One) {
      template ("Check out this #{concept(this)}.")
    }
    default {
      template ("I found these #{concept(this)}.")
    }
  }
}
