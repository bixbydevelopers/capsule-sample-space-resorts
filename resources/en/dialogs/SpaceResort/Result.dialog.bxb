dialog (Result) {
  match : core.Boolean(all) {
    from-property: SpaceResort (this) {
      from-output: FindSpaceResorts (find)
    }
    }
  if (size(this) == 1) {
    template ("Check out this #{concept(this)}.")
  } else {
    template ("I found these #{concept(this)}.")
  }
}
