// TODO: Should be property result, but that doesn't work
dialog (Result) {
  match: Gravity (this)
  if (size(this) == 1) {
    template-macro (GravityResultDetails) {
      param (gravity) {
        expression (this)
      }
    }
  } else {
    // TODO
    template (TODO)
  }
}