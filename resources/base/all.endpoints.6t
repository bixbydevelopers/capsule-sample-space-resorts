endpoints {
    authorization {
      none
    }
    
  action-endpoints {
    action-endpoint (FindSpaceResorts) {
      accepted-inputs (planet, searchCriteria)
      local-endpoint (FindSpaceResorts.js)
    }
  }
}
