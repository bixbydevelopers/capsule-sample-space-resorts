endpoints {
    authorization {
      none
    }
    
  action-endpoints {
    action-endpoint (FindSpaceResorts) {
      accepted-inputs (planet, sort)
      local-endpoint (FindSpaceResorts.js)
    }
  }
}
