action (FindSpaceResorts) {
  description (Find space resorts)
  type (Search)
  collect {
    input (planet) {
      type (Planet)
    }
    input (sort) {
      type (Sort)
    }
  }
  output (SpaceResort)
}
