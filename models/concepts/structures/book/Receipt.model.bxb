structure (Receipt) {
  description (Receipt)
  property (habitatPod) {
    type (HabitatPod)
    min (Required)
    visibility (Private)
  }
  property (self) {
    type (self.SelfInfo)
    min (Required)
    visibility (Private)
  }
}
