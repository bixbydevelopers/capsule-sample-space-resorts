structure (Receipt) {
  description (Receipt)
  property (item) {
    type (Item)
    min (Required)
    visibility (Private)
  }
  property (self) {
    type (self.SelfInfo)
    min (Required)
    visibility (Private)
  }
}
