action (CommitOrder) {
  description (Commit order)
  type (Commit)
  confirmed-by (common.Confirmation)

  collect {
    input (order) {
      type (Order)
      min (Required)
    }
  }
  output (Receipt)
}
