action (CommitOrder) {
  description (Commit order)
  type (Commit)

  confirm {
    by (Confirmation)
  }

  collect {
    input (order) {
      type (Order)
      min (Required)
      default-init {
        intent {
          goal: CreateOrder
        }
      }
    }
  }
  output (Receipt)
}
