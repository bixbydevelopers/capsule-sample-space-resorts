action (CommitOrder) {
  description (Commit order)
  type (Commit)

  confirmed-by (Confirmation) {
    mode (Transactional)
    confirm-label {
      template("Buy")
    }
  }

  collect {
    input (order) {
      type (Order)
      min (Required)
    }
  }
  output (Receipt)
}
